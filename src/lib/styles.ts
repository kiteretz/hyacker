// 変数定義で使用 Prettierの整理対象に含める
// import { styles } from '../../lib/styles'; のようにして読み込み
// ex: const borderStyle = styles('border-2 border-solid border-black');
export const styles = (strings: string): string => strings;
