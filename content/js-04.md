---
title: JavaScript のイベントリスナーの使い方
description: addEventListener を使ったイベント処理の基本と、イベントの伝播（バブリング・キャプチャリング）の仕組みを解説します
upDate: 2025-01-23
pubDate: 2025-01-23
author: とも
image:
  url: /assets/posts/background.svg
  alt: 背景の画像
tags:
  - JavaScript
status: publish
---

## 回答

`addEventListener` でイベントリスナーを登録し、特定のイベントが発生したときに処理を実行します。

```js
const button = document.querySelector('#myButton');

// クリックイベント
button.addEventListener('click', (event) => {
  console.log('クリックされました');
  console.log(event.target); // クリックされた要素
});

// フォームの送信
const form = document.querySelector('form');
form.addEventListener('submit', (event) => {
  event.preventDefault(); // デフォルトの送信を防ぐ
  // バリデーション処理など
});
```

## 解説

### よく使うイベント

- `click` : クリック
- `input` : 入力値変更
- `change` : 変更確定
- `submit` : フォーム送信
- `keydown` / `keyup` : キー操作
- `mouseover` / `mouseout` : マウスオーバー
- `scroll` : スクロール
- `resize` : ウィンドウサイズ変更
- `DOMContentLoaded` : DOM 読み込み完了

### イベントの伝播（バブリング）

子要素でイベントが発生すると、親要素へと伝播します。

```js
// 親要素でイベントを捕捉（イベント委譲）
document.querySelector('.list').addEventListener('click', (e) => {
  if (e.target.matches('li')) {
    console.log('リストアイテムがクリックされました:', e.target.textContent);
  }
});
```

### リスナーの削除

```js
const handleClick = () => console.log('clicked');
button.addEventListener('click', handleClick);

// 後で削除
button.removeEventListener('click', handleClick);

// 1回だけ実行
button.addEventListener('click', handleClick, { once: true });
```
